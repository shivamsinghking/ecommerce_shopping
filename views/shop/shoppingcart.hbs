{{#if product}}
{{#each product}}
<div class="row">
    <div class="col-sm-6 col-md-6 col-md-offset-3 col-sm-offset-3">
     <ul class="list-group">
      
         <li class="list-group-item">
             <span class="badge">{{this.item.title}}</span>
             <strong>{{this.item.title}}</strong>
             <span class="label label-success">${{this.price}}</span>
             <div class="btn-group">
                 <button class="btn btn-primary btn-xs dropdown-toggle" type="button" data-toggle="dropdown">Action<span class=" caret"</button>
                <ul class="dropdown-menu">
                    <li><a href="#">Reduce by 1</a></li>
                    <li><a href="#">Remove all</a></li>
        
                
                </ul>            
             
             </div>
         </li>
     </ul>
     </div>
     </div> 
     {{/each}}
     <div> total price {{totalPrice}}</div>
     <div><a href="/user/checkout"> </div><button type="button" class="btn btn-success">check out</button></a>
     {{else}}
     <div> no products!</div>
     {{/if}}